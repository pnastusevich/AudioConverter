import SwiftUI

struct SubscriptionView: View {
    
    @StateObject var viewModel: SubscriptionViewModel
    
    var body: some View {
        Text("Subscription")
        
        Button("Finish") {
            
        }
    }
}

